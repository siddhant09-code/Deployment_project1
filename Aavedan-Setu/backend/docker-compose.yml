services:
  postgres:
    image: postgres:16

    container_name: gov_postgres

    restart: unless-stopped

    environment:
      POSTGRES_DB: gov_assist_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123

    ports:
      - "5433:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: