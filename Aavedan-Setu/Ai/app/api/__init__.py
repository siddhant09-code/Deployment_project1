"""
api/

FastAPI routes and HTTP-layer concerns only: request/response
mapping, dependency wiring (deps.py), and exception-to-HTTP-status
mapping (error_handlers.py). No business logic — see services/.
"""
